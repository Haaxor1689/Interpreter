func foo(var a, var b) {
    if ==(a, b) {
        return 0;
    } elseif >(a, b) {
        return 1;
    } elseif <(a, b) {
        return -1;
    } else {
        # Should never happen
        return -2;
    }
	 return -3;
}