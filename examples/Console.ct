func Text(): string {
	WriteLine("Write text:");
	var a = ReadText();
	WriteLine(+ "You wrote " a);
	return a;
}

func Number(): number {
	WriteLine("Write number:");
	var a = ReadNumber();
	return a;
}